/**
 * Content Collections Configuration
 *
 * Note: Questions are now loaded dynamically from the WorldExams API
 * (https://worldexams.pages.dev/api/v1). The local content collection
 * has been removed in favor of the centralized question bank.
 *
 * This file is kept for potential future local content like blog posts.
 */

// No collections defined - all question content is centralized in worldexams
export const collections = {};
