# Country Analysis Task for Codex

You are a senior full-stack developer analyzing the WorldExams Astro platform for country-specific hardcoded content.

## MISSION
Analyze the entire saberparatodos/src directory and find ALL hardcoded references to:
- "ICFES", "Colombia", "Saber 11", "Saber 3", "Saber 5"
- "colombiano", "colombiana"
- Hardcoded exam names that should use the country's exam name dynamically
- Hardcoded dates/frequencies that are Colombia-specific (like "Convocatoria ICFES")
- Any content that assumes the user is in Colombia when it should be country-aware

## FILES TO ANALYZE
1. All .astro files in src/pages/
2. All .svelte files in src/components/
3. All .ts files in src/lib/
4. src/config/exam-guide-content.ts (this likely has country-specific content)
5. src/config/countries.config.ts (verify it has all countries)

## OUTPUT
For each distinct issue found, create a GitHub issue using the gh cli in the repo E:\scripts-python\worldexams:

```bash
cd E:\scripts-python\worldexams
gh issue create --title "fix(country): [BRIEF_TITLE]" --body "## Problem
[BRIEF_DESCRIPTION_OF_THE_HARDCODED_CONTENT]

## Location
File: [path]
Line/context: [specific code]

## Suggested Fix
[BRIEF_SUGGESTION]

## Country Config Available
The countries.config.ts has: CO (ICFES), MX (PLANEA/EXANI), AR (APRENDER), CL (SIMCE), PE (ECE), EC (SENESCYT), BR (ENEM)
" --label "country-multi" --label "tech-debt"
```

Group similar issues together (e.g., if 5 components have the same problem, create ONE issue for all of them).

After creating all issues, also create ONE summary meta-issue:
```bash
cd E:\scripts-python\worldexams
gh issue create --title "feat(country): Make entire platform country-aware (meta-issue)" --body "## Summary
This meta-issue tracks the effort to make the entire SaberParaTodos platform respond to the user's detected country with country-specific content.

## Country Detection
- Middleware: detects country via Cloudflare headers or ip-api.com
- Cookie override: country=XX
- localStorage cache for client-side persistence

## Countries Configured
- CO: ICFES Saber (Colombia)
- MX: PLANEA / EXANI (Mexico)
- AR: APRENDER (Argentina)
- CL: SIMCE (Chile)
- PE: ECE (Peru)
- EC: SENESCYT (Ecuador)
- BR: ENEM (Brasil)

## Issues Created
[List all issue numbers and titles created]

## Progress
- [ ] Pages: make all pages country-aware
- [ ] Components: make all Svelte components country-aware
- [ ] Config: ensure exam-guide-content.ts is fully multi-country
- [ ] Benchmarks: make icfes-benchmarks.ts generic per country
" --label "country-multi"
```

When done, output a summary of ALL issues created (numbers and titles).
