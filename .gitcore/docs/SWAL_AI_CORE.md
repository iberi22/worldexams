# SWAL AI Core — copia operativa

Ver canónico: `docs/SWAL/AI_CORE.md` (monorepo).

Resumen: `createAiCore({ mesh, instanceId })` en edge-mesh; sin LLM hosted; descarga con consentimiento; WorldExams consume vía `/ajustes/ia`, tutor y generador local.
