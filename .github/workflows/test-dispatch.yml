# Test workflow dispatch
name: Test Dispatch

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Test message'
        required: false
        type: string
        default: 'Hello World'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Message: ${{ inputs.message }}"
